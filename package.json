{
  "name": "melodymath",
  "private": true,
  "description": "Hebrew math practice and graph sonification — no install, no backend",
  "scripts": {
    "test": "node --test",
    "build": "node scripts/build-site.js"
  }
}
